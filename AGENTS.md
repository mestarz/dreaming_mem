# 仓库工作约定

## 临时数据

- 临时文件、中间产物、调试输出、缓存和可重新生成的数据统一放在 `/tmp` 中。
- 不要将仅供单次任务使用的临时数据写入 `workdata/`。

## 固化工作数据

- `workdata/` 用于存放不纳入 Git 跟踪、但需要长期保留和反复使用的数据集与固定脚本。
- `workdata/` 中的内容不是临时缓存；除非用户明确要求，否则不要删除、覆盖或批量清理。
- 对 `workdata/` 中的数据或脚本进行格式转换时，应保留其稳定、可复现和可再次执行的用途。
